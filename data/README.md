# data/

- `raw/` — datasets descargados (MNIST/EMNIST vía torchvision; luego NIST SD19). **Gitignored.**
- `processed/` — datos preprocesados. **Gitignored.**

Descarga: `python scripts/download_data.py --dataset mnist`
