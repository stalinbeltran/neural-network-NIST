# Estrategia: MLP sobre la IMAGEN COMPLETA (todos los píxeles 28x28).
name: mlp_full
extends: ../base.yaml

# transform ausente => imagen completa, input_shape = (1, 28, 28)

model:
  name: mlp
  hidden: [128]
  dropout: 0.0
